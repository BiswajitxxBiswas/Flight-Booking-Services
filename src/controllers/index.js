/**
 * To make the code more clean we make a new index.js in contollers and import all the contoller here and then when we want to us we just have to import this index.js once 
 */

module.exports = {
   infoContoller : require('./info-controller')
}